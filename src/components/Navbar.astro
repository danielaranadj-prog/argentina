---
/* Navbar Final: Tu Logo SVG + Identidad Argentina */
---
<nav class="navbar">
  <div class="nav-container">
    
    <a href="/destinos_argentina" class="logo-wrapper">
      
      <img src="/destinos_argentina/logo.svg" alt="Instante Trips Logo" class="adaptive-logo" />

      <div class="identity-badge">
        <span class="badge-divider"></span>
        <span class="flag">🇦🇷</span>
        <span class="region">ARG</span>
      </div>
    </a>

    <button id="menu-toggle" class="menu-btn" aria-label="Abrir menú">
      <i class="fas fa-bars"></i>
    </button>
  </div>

  <div id="mobile-menu" class="mobile-nav">
    <div class="menu-header">
      <span class="menu-title">MENÚ PRINCIPAL</span>
    </div>
    <div class="menu-links">
      <a href="/destinos_argentina/#destinos" class="menu-link">
        <i class="fas fa-map-marked-alt"></i> Destinos
      </a>
      
      <a href="/destinos_argentina/tours" class="menu-link">
        <i class="fas fa-ticket-alt"></i> Experiencias
      </a>

      <a href="/destinos_argentina/planifica" class="menu-link">
         <i class="fas fa-question-circle"></i> Planifica tu viaje
      </a>
    </div>
  </div>
</nav>

<script>
  // Tu script original intacto
  const menuBtn = document.getElementById('menu-toggle');
  const mobileMenu = document.getElementById('mobile-menu');
  const icon = menuBtn?.querySelector('i');

  const toggleMenu = () => {
    mobileMenu?.classList.toggle('active');
    if (mobileMenu?.classList.contains('active')) {
      icon?.classList.remove('fa-bars');
      icon?.classList.add('fa-times');
    } else {
      icon?.classList.remove('fa-times');
      icon?.classList.add('fa-bars');
    }
  };

  menuBtn?.addEventListener('click', (e) => {
    e.stopPropagation();
    toggleMenu();
  });

  document.addEventListener('click', (e) => {
    const target = e.target as HTMLElement;
    if (mobileMenu?.classList.contains('active') && !mobileMenu.contains(target) && !menuBtn?.contains(target)) {
      toggleMenu();
    }
  });
</script>

<style>
  /* --- ESTILOS BASE (Tus variables) --- */
  .navbar {
    position: fixed;
    top: 0;
    width: 100%;
    height: 70px;
    background: var(--nav-bg, rgba(255,255,255,0.9));
    backdrop-filter: blur(15px);
    -webkit-backdrop-filter: blur(15px);
    z-index: 2000;
    display: flex;
    align-items: center;
    border-bottom: 1px solid var(--border, rgba(0,0,0,0.1));
    padding-top: env(safe-area-inset-top);
  }

  .nav-container {
    width: 100%;
    max-width: 1100px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 20px;
  }

  /* --- NUEVOS ESTILOS PARA LOGO SVG + BANDERA --- */
  .logo-wrapper {
    display: flex;
    align-items: center;
    gap: 12px; /* Espacio entre tu logo y la bandera */
    text-decoration: none;
    color: var(--title, #000); /* Para el color del texto ARG */
  }

  /* Estilo para tu imagen SVG */
  .adaptive-logo {
    height: 32px; /* Altura controlada */
    width: auto;
    transition: filter 0.3s ease;
  }

  /* La Insignia de Argentina */
  .identity-badge {
    display: flex;
    align-items: center;
    gap: 6px;
    height: 28px;
    padding-left: 2px;
  }

  .badge-divider {
    width: 1px;
    height: 80%; /* Línea un poco más baja que el logo */
    background-color: var(--border, #ccc);
    margin-right: 4px;
    opacity: 0.7;
  }

  .flag {
    font-size: 1.2rem;
    line-height: 1;
  }

  .region {
    font-family: 'Montserrat', sans-serif;
    font-size: 0.75rem;
    font-weight: 700;
    opacity: 0.6;
    letter-spacing: 0.5px;
  }

  /* --- TUS ESTILOS ORIGINALES DEL MENÚ (Intactos) --- */
  .menu-btn {
    background: none;
    border: none;
    font-size: 1.5rem;
    cursor: pointer;
    color: var(--title);
    padding: 5px;
  }

  .mobile-nav {
    position: fixed;
    top: 0;
    right: -100%;
    width: 70%; 
    max-width: 300px;
    height: 100vh;
    background: var(--card-bg, #fff);
    transition: right 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    padding: 100px 30px; 
    z-index: 1999;
    box-shadow: -10px 0 30px rgba(0,0,0,0.1);
    display: flex;
    flex-direction: column;
  }

  .mobile-nav.active { 
    right: 0;
  }

  .menu-header { 
    margin-bottom: 30px; 
    border-bottom: 1px solid var(--border); 
    padding-bottom: 10px; 
  }

  .menu-title { 
    font-size: 0.7rem; 
    color: var(--gold, #D4AF37);
    font-weight: 800; 
    letter-spacing: 2px; 
    text-transform: uppercase;
  }

  .menu-links { 
    display: flex; 
    flex-direction: column; 
    gap: 25px; 
  }

  .menu-link { 
    font-size: 1.3rem; 
    font-weight: 700; 
    color: var(--text, #333);
    display: flex; 
    align-items: center; 
    gap: 15px; 
    text-decoration: none;
    transition: color 0.2s;
  }

  .menu-link i { 
    color: var(--gold, #D4AF37); 
    width: 25px; 
    text-align: center; 
  }

  /* Mantiene tu lógica para invertir colores en modo oscuro si usas esas variables */
  @media (prefers-color-scheme: light) {
    .menu-btn { filter: brightness(0); }
  }
  @media (prefers-color-scheme: dark) {
    .menu-btn { filter: brightness(0) invert(1); }
  }
</style>